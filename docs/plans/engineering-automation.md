# CloseGraph minimal engineering setup

The custom listener/controller plan is superseded by the user's preference to use existing Hermes cron and avoid spending more time on orchestration. Abandoned untracked modules are preserved locally under ignored `var/abandoned-custom-controller/`, not shipped.

Deliver a paused native cron setup, tested macOS sandbox, read-only GitHub change monitor, architecture/review documentation and the complete epic/task issue map. Do not implement the financial application or start unattended model/merge jobs.

1. Keep seven product ownership epics plus E0 delivery setup. Verify each original OpenSpec task is mapped once and task dependencies form a DAG.
2. Register engineering/reviewer cron definitions paused, local-only; default-profile unrelated jobs remain unchanged. No public listener or new daemon. Keep execution unavailable until a supervised real adapter/publishing pilot.
3. Retain real full-clone/filesystem/network sandbox tests. Run all retained automation tests and OpenSpec strict validation.
4. Publish and read back the private GitHub catalogue, native parent/child relationships and documentation commit.
5. Report actual evidence, paused state and remaining adapter/reviewer/protection gates. Product tests are not counted as run merely because their issue bodies name commands.
